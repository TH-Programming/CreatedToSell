
<%= render partial: "users/info", locals: {user: @creator} %>

<%= @creator.merchandise.each do |merch|%>
<%= link_to merch.title, creator_merchandise_path(merch) %>
<%= merch.price %>
<%= merch.description%>
<%= end %>