<h1>New Sale</h1>
<% if @sale.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@sale.errors.count, "error") %>
      prohibited this sale from being saved:
    </h2>
 
    <ul>
    <% @sale.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= @merchandise.title %> <>
<%= @merchandise.price %>
<%= form_for @sale do |f|%>
<%# <%= f.hidden_field :user_id, value: current_user.id %>
<%= f.hidden_field :merchandise_id, value: params[:merchandise_id] %>
If the seller has given you a PromoCode please enter it here.
<%= f.text_field :promo_code %>
<%= f.submit "Place Order" %>
<% end %>

